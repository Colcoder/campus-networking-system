# MKU ALUMNI NETWORK

### Setting up

- In project directory run:

  ```
   npm install
  ```

- Followed by:

  ```
  npm start
  ```

- To run in development with hot reloading:
  ```
  npm run start:dev
  ```
